# AssetAgent 产品策划文档

| 项目 | 内容 |
|---|---|
| 工作代号 | AssetAgent |
| 开发主体 | 广州尼特之家信息技术有限公司 |
| 版本 | v0.8 草案 |
| 变更记录 | v0.3 新增 7.8 订阅与官方中转服务（BYOK/中转双模式）；v0.4 市场确定（国内 + 英语地区双市场）；v0.5 订阅改为个人/企业双线（结构参考智谱 GLM：个人自助月/年付、企业合同制对公采购）；v0.6 确立开发主体：广州尼特之家信息技术有限公司；v0.7 产品更名为 AssetAgent（原 AssetForge 与现有软件重名），同步 1.1、7.6 目录名与项目记忆；v0.8 新增 5.4 MVP 范围冻结（Must/Should/Won't 分层、状态机与数据模型、发布门槛），重写 9 章为关键路径 + M0–M6 节点分解 + 人力与埋点方案 |
| 日期 | 2026-09-11 |
| 状态 | 待评审 |
| 面向读者 | 创始团队 / 投资人 / 首批种子用户 |

---

## 1. 项目概述

### 1.1 一句话定位

**AssetAgent 是一款面向游戏美术的桌面 Agent：把概念图变成引擎就绪的 3D 道具与环境资产，自动完成后处理（减面 / UV / 烘焙 / 校验 / 导出），10 分钟出白模。**

### 1.2 背景与机会

- 生成式 3D（image-to-3D）在 2025 年后达到"道具级可用"质量，但现有工具（Meshy / Tripo / Rodin 等）普遍止步于"给一个粗糙网格 + 基础贴图"，**美术最头疼的后处理链路（拓扑修复、UV、烘焙、引擎导出规范）全部要人肉完成**。
- 这条后处理链路恰好是 Agent 最擅长的：工具链成熟（Blender headless / pymeshlab / xatlas 全开源）、规则明确（各引擎有明确的资产规范）、可自动校验。
- 游戏美术手里永远有概念图。**"概念图 → 引擎"的最后一公里没有被任何产品认真打通**，这是我们的机会。

### 1.3 设计原则

1. **美术是"挑选者"，不是 prompt 工程师**：输入以图为主、文字为辅；每次生成多变体，美术挑，而不是接受唯一结果。
2. **交付标准按美术的职业习惯定**：面数预算、四边面、UV 规范、真实单位、命名规范——不合规范的产出美术会直接丢弃。
3. **永不覆盖用户文件**：所有产物写入独立输出目录 + 版本号，源概念图只读。
4. **每个结果可追溯**：版本树记录 prompt、参数、模型来源、校验报告，可复现、可回滚。

---

## 2. 目标用户与竞品

### 2.1 用户画像

| 画像 | 场景 | 核心诉求 |
|---|---|---|
| 独立开发者 / 小团队美术 | 一人身兼程序美术，场景里需要大量道具 | 快、便宜、能直接进 Unity/UE |
| 中大型工作室 3D 场景美术 | 负责关卡陈设（Prop Art），需求量 50–200 件/月 | 风格一致、符合资产规范、可二次精修 |
| 关卡设计师 | 白盒搭建阶段需要 Blocker/灰盒 | 极速出可进引擎的体块，不求精 |

### 2.2 核心痛点

1. 3D 生成工具的输出**拓扑是三角面汤、无 UV 规范**，进引擎前要花 1–3 小时手修，比手做白模还慢。
2. 资产包风格不一致：一个场景 20 件道具，用现有工具逐个生成会得到 20 种画风。
3. 后处理（减面、UV、烘焙、LOD、命名、单位）是重复劳动，没有工具把它自动化并按引擎规范交付。

### 2.3 竞品格局与差异化

| 竞品 | 定位 | 短板（我们的机会） |
|---|---|---|
| Meshy / Tripo / Rodin | 云端 text/image-to-3D 生成 | 只到"生成"为止；无后处理管线；无引擎导出规范；Web 端无本地文件管理 |
| Kaedim | 人工精修的 3D 生成服务 | 贵（单件数十美元级）、按服务计费、交付周期长 |
| Sloyd | 参数化模板资产 | 模板库有限，自由度低 |
| TRELLIS / Hunyuan3D（开源） | 本地生成模型 | 无产品化封装，需要工程能力自行搭管线 |
| Blender 加人工 | 现状 | 慢，但美术信任 |

**差异化定位：不做"又一个生成工具"，做"生成之后的整条交付管线"。** 生成引擎做成可替换的 Provider（云端三家 + 本地开源），管线和校验才是我们的护城河。

---

## 3. 产品范围

### 3.1 做（In Scope）

- 硬表面道具、场景陈设资产（武器、箱柜、建筑构件、植被优化形态等）
- 关卡灰盒 Blocker 批量生成
- 资产包级批量生成（风格锁定）
- 后处理自动化：网格修复、减面、四边面重拓扑、UV 展开与烘焙、LOD 链生成
- Game-ready 自动校验与报告
- Unity / Unreal / 通用 glTF 三套导出预设

### 3.2 不做（Out of Scope，v1 明确排除）

- **角色 / 生物 / 蒙皮资产**：自动重拓扑对有机体不成熟，做了砸招牌；待技术成熟后评估
- 刚体动画 / 骨骼绑定（Phase 3 再看，第三方 API 已有能力）
- 场景级整体生成（房间/地块）——先做单资产，组合交给引擎
- 2D 素材（原画、图标、UI）——作为后续独立模块，不进 MVP

### 3.3 产品形态

Windows 优先（游戏行业主力平台）的桌面应用：Electron 壳 + 本地 Python 服务 + 云端生成 API。本地不需要独显也能跑全流程（生成走云端），有 24GB 级显卡的工作站可切换本地模型（Phase 2）。

---

## 4. 核心工作流

### 4.1 工作流 A：概念图 → 引擎白模（杀手场景）

1. 美术拖入一张概念图（支持补充正/侧视图，多视图输入显著提升质量）
2. 选择资产规格预设：面数预算、是否要四边面、目标引擎、单位
3. 云端 image-to-3D 生成 2–3 个变体（约 1–3 分钟）
4. 3D 视口对比变体 → 选中一个
5. 后处理管线自动执行：修复 → 减面 → UV → 烘焙 → 校验
6. 校验报告通过 → 一键导出 FBX+贴图 / GLB，可直接拖进 Unity/UE
7. 全程目标：**≤10 分钟，全程不写一个 prompt 字**

### 4.2 工作流 B：文本 → 资产包

1. 美术用自然语言描述一套资产："荒漠废弃哨站，8 件道具，写实风格，面数预算 3000/件"
2. Planner 拆解清单 → 先生成 1 张风格设定图，美术确认风格
3. 逐件生成（风格设定图作为参考图条件），每件走工作流 A 的 3–7 步
4. 整包一致性终检（Critic 横向对比），打包输出，附资产清单 CSV

### 4.3 工作流 C：对已有网格做后处理（引流功能）

美术把**任何来源**的粗糙网格（自己扫的、其他 AI 工具生成的）拖进来，只跑修复/减面/UV/烘焙/导出。这条工作流不依赖生成 API，零成本，是低门槛获客入口，也是让美术信任校验器的方式。

---

## 5. 功能规格（MVP）

### 5.1 功能清单

| 优先级 | 功能 | 说明 |
|---|---|---|
| P0 | 概念图/文本输入 | 拖拽上传、多视图、规格预设选择 |
| P0 | 云端 image-to-3D | Meshy/Tripo 二选一起步，Provider 抽象层；多变体 |
| P0 | 后处理管线 | bpy headless：修复/减面/重拓扑/UV/烘焙 |
| P0 | Game-ready 校验器 | 见 7.4 规则表；报告与 3D 视图联动高亮 |
| P0 | 导出 | FBX（Unity/UE 预设）、GLB、贴图包；命名规范 |
| P0 | 资产库 | 本地目录扫描 + 标签 + 版本树 |
| P0 | 转台预览 | bpy 渲染转台图/白模图，供人工与 Critic 检查 |
| P0 | 3D 检查视口 | three.js/r3f：相机、clay/线框/材质通道、HDRI 环境、gizmo 摆放（详见 7.7） |
| P1 | 贴图查看器与 2D/3D 联动 | RGBA 通道分离、透明底棋盘格；视口点选面 → 高亮对应 UV 岛（详见 7.7） |
| P1 | 概念图 mask 绘制 | 画笔圈选区域 → 生成 API inpaint 局部重绘 |
| P0 | BYOK 管理 | 设置页填写各 Provider 自有 API Key，本地加密存储；无 Key 时生成功能不可用并引导 |
| P2 | 账号 / 订阅 / 官方中转 | 邮箱登录、订阅支付；登录且订阅后走官方中转扣额度，额度面板与用量台账（详见 7.8） |
| P2 | 企业管理后台（Web） | 成员/席位管理、团队额度池分配、用量审计报表、合同与发票记录（企业版配套，详见 8.2） |
| P1 | 中英双语（i18n） | 双市场定位；框架 MVP 即预留（react-i18next），首发 zh / en |
| P1 | 资产包批量（工作流 B） | Planner + 风格设定图锁定 |
| P1 | LOD 链生成 | LOD0–LOD3 自动减面 + 引擎命名 |
| P1 | 后处理工作流（工作流 C） | 纯后处理入口 |
| P2 | PBR 贴图增强 | 扩散模型重生成高分辨率 albedo → 投影烘焙 |
| P2 | 本地生成模型 | TRELLIS（MIT）优先，Hunyuan3D 备选 |

### 5.2 页面结构

```
┌─ 工作台（创建入口：拖图 / 输入文本 / 规格预设 / 概念图 mask 圈选重绘）
├─ 资产库（网格视图：缩略图、标签、状态徽章——待处理/已校验/已导出）
│   └─ 资产详情
│       ├─ 3D 视口（PBR 预览 + 线框切换 + UV 检查器视图）
│       ├─ 版本树 / 变体对比（并排 A/B）
│       ├─ 校验报告（规则逐条 PASS/FAIL，点击高亮问题面/UV岛）
│       └─ 导出面板（引擎预设、LOD、贴图格式）
├─ 批量任务（资产包进度看板）
└─ 设置（API Key、成本预算上限、导出预设编辑、缓存管理）
```

### 5.3 关键交互决策

- **变体制**：每次生成默认 2–3 变体（预算可调），美术挑选后才进入后处理；所有变体保留在版本树。
- **校验报告先行**：导出前强制展示校验报告；FAIL 项给出"自动修复 / 忽略 / 重生成"三选。
- **成本可见**：生成前预估本次花费，设置页有月度预算上限，超限需确认。
- **视口分层**：前端只管检查与摆放（非破坏性），一切破坏性修改走后端管线并产生新版本；撤销 = 回到版本树上一节点，不自建 undo 栈。

> **MVP 范围以 5.4 为准。** 5.1 的 P0/P1/P2 是产品全量优先级（含 Phase 2/3），不等于 MVP 范围；两者冲突处按 5.4.3 处理。

### 5.4 MVP 范围冻结

#### 5.4.1 MVP 的唯一目标

**让 3–5 名外部美术在无人协助的情况下，把一张概念图变成能直接拖进 Unity / UE 的白模资产，全程 ≤10 分钟、不写一个 prompt 字。**

所有功能取舍以这条线为准，用三个问题判定，任一为"否"即不进 MVP：

1. 没有它，工作流 A 能否端到端跑完？
2. 它是否直接决定"美术愿不愿意用第二个资产"？
3. 8 周内能否由现有团队交付且质量可接受？

#### 5.4.2 功能分层

**Must（不可砍，缺一项即不可发布）**

| # | 功能 | 最小可接受标准 |
|---|---|---|
| M1 | 输入与规格预设 | 拖拽/选择 png·jpg·webp（≤20MB）；可选补正/侧视图；预设含面数预算、目标引擎、单位；预设可存为项目配置 |
| M2 | 云端 image-to-3D | 单 Provider 起步 + 抽象层；一次生成 2–3 变体；单次 ≤3 分钟；失败有可读错误与重试；切 Provider 不需重启 sidecar |
| M3 | 后处理管线 v1 | 修复（补洞/去孤立组件/法线统一）→ 减面至预算内 → UV 展开（零重叠）→ 烘焙 normal/AO；每步落中间文件与日志；单件本地全流程 ≤5 分钟 |
| M4 | 校验器 v1 | 面数 / N-gon / UV 重叠 / UV 岛间距 / 轴心 / 单位 / 命名 共 7 条规则；输出结构化 JSON + 人读报告；FAIL 可定位到具体面或 UV 岛 |
| M5 | 导出 v1 | FBX（Unity Y-up 米制、UE Z-up 厘米两套预设）+ GLB + 贴图包；命名按预设正则；导出目录结构固定；Unity 2022 LTS 与 UE5 实机导入无报错 |
| M6 | 转台渲染 | 8 帧转台 + 1 张白模图，PNG 输出；供人工挑选，并作为后续 Critic 视觉通道的输入 |
| M7 | 3D 检查视口 v1 | 加载 GLB；轨道相机；clay/线框/材质切换；HDRI 环境预设；包围盒与尺寸标注（gizmo 摆放、BVH 拾取延后） |
| M8 | 资产库与版本树 v1 | 本地目录扫描入库；缩略图；标签；一次任务 = 一个资产；变体/后处理/导出均落为版本节点，可回溯与回滚；源概念图只读 |
| M9 | BYOK 设置 | 按 Provider 分栏填写 Key，OS 级加密存储；连通性自检按钮；无 Key 时生成入口禁用并引导；Key 永不上传 |
| M10 | 成本可见 | 生成前预估本次花费；月度预算上限；超限二次确认；本地用量流水（MVP 不上云） |
| M11 | 工作流 C 入口 | 拖入任意来源网格 → 复用 M3/M4/M5 管线。UI 增量极小，却是生成失败时的兜底，也是让美术信任校验器的入口 |

**Should（MVP 内争取；进度紧张时整体延到 Phase 1.5，不阻塞发布）**

| # | 功能 | 说明 |
|---|---|---|
| S1 | 四边面重拓扑 | **须以 W1–W2 Spike 结论为准**；对硬表面道具效果不达标则整项延后，MVP 以"三角面 + 面数预算达标"交付 |
| S2 | 贴图查看器 | RGBA 通道分离、透明底棋盘格 |
| S3 | 2D/3D 联动定位 | 点选面 → 高亮对应 UV 岛（见 7.7） |
| S4 | 变体并排 A/B 对比 | MVP 先做单视口切换，并排对比延后 |
| S5 | LOD 链 | LOD0–LOD2 + 引擎命名 |
| S6 | 概念图 mask 重绘 | 画笔圈选 → inpaint 局部重绘 |
| S7 | i18n 文案 | 框架 MVP 即预留（react-i18next）；en 全量翻译不阻塞发布 |

**Won't（明确不在 MVP，产品内以"即将推出"呈现）**

账号 / 登录 / 订阅 / 支付 / 官方中转 · 资产包批量（工作流 B）· 本地生成模型 TRELLIS · PBR 贴图增强 · 角色 / 生物 / 蒙皮（v1 永久排除）· 场景级整体生成 · Unity / UE 编辑器插件 · 企业 Web 管理后台 · 2D 素材生成

#### 5.4.3 与 5.1 的三处冲突修正（需评审确认）

1. **账号 / 订阅 / 官方中转**：5.1 标 P2，7.8 说"Phase 2 落地"——统一为 **Phase 2**。MVP 只落地"路由开关"这一个配置项，不做登录、不做支付、不做 relay。
2. **四边面重拓扑**：5.1 标 P0，但它是整条管线里技术不确定性最高的一环（自动四边面重拓扑对硬表面道具的实际质量未经本项目验证）——降级为 **Should + W2 末决策点**，避免它拖垮关键路径。
3. **3D 检查视口**：5.1 标 P0，MVP 收窄为 M7 子集（只做检查与摆放，不做顶点级编辑，符合 7.7 的边界）。

#### 5.4.4 MVP 必须冻结的两个接口（否则前后端必然返工）

**资产状态机**（W1 冻结，驱动资产库徽章与筛选）

```
草稿 → 生成中 → 待挑选 → 后处理中 → 待校验 → 已校验 → 已导出
                     ↘ 失败（可重试）        ↘ 已归档
```

**数据模型（最小字段）**

```json
{
  "asset":   { "id", "style_id", "source": "image|text|mesh", "status", "created_at" },
  "variant": { "id", "asset_id", "provider", "params", "mesh_path", "turntable_paths" },
  "version": { "id", "asset_id", "parent_id", "op", "params", "mesh_path", "created_at" },
  "job":     { "id", "asset_id", "step", "status", "progress", "cost", "error" },
  "report":  { "id", "version_id", "rules": [{ "rule", "result", "value", "threshold", "locator" }] },
  "export":  { "id", "version_id", "preset", "files": [], "created_at" },
  "provider_config": { "provider", "mode": "byok|relay", "key_ref" }
}
```

#### 5.4.5 MVP 发布门槛（Release Gate，全部通过才交付种子用户）

| # | 门槛 | 判定方式 |
|---|---|---|
| G1 | 黄金集 10 张概念图，TTFE P50 ≤ 10 分钟（含人工挑选时间） | 实测 |
| G2 | 校验器一次通过率 ≥ 70%（含一轮自动重试） | 实测 |
| G3 | 导出资产在 Unity 2022 LTS 与 UE5 实机导入无报错，贴图通道正确 | 实机验证 |
| G4 | 连续 20 次任务无崩溃，无 P0/P1 级 bug | 压力跑 |
| G5 | 成本预估与实际花费偏差 ≤ 20% | 对账 |
| G6 | 3–5 名外部美术不看文档、不看录屏独立跑通，成功率 ≥ 80% | 外测 |
| G7 | 源概念图未被修改；所有产物位于独立输出目录且带版本号 | 文件检查 |

---

## 6. Agent 设计

### 6.1 角色划分

| 角色 | 模型 | 职责 |
|---|---|---|
| Planner | 文本 LLM（云 API） | 资产包任务拆解、生成参数建议、失败重试策略 |
| Critic（视觉通道） | 多模态 LLM | 对转台渲染图评分：形状符合度、贴图错位、风格偏移，输出结构化结论 |
| Critic（程序通道） | 规则引擎（非模型） | 执行 7.4 校验规则，输出客观 PASS/FAIL |

双通道都通过才标记"已校验"；任一失败进入自动重试（改 prompt/参数），2 轮仍失败则降级为人工挑选变体。**不追求全自动，追求"自动的都可信，剩下的交给美术挑"。**

### 6.2 记忆与风格圣经

- 每个"风格包"是一份可复用配置：风格设定图 + prompt 模板 + 参数 + LoRA/参考图（Phase 2）
- 资产记录数据模型：`{ id, style_id, 来源(图/文), lineage(父版本), 生成参数, mesh 文件, 校验报告, 导出记录 }`
- 跨会话复用：同风格包生成新资产时自动注入历史规范

---

## 7. 技术方案

### 7.1 总体架构

```
┌────────────────────────────────────────────┐
│  Electron 桌面壳（React + react-three-fiber） │
│  工作台 / 资产库 / 3D视口 / 校验报告 / 导出      │
└──────────────┬─────────────────────────────┘
               │ HTTP (localhost)
┌──────────────▼─────────────────────────────┐
│  Python FastAPI sidecar                     │
│  ┌─────────┐  ┌──────────────────────────┐ │
│  │ Agent 层 │  │ 工具层                    │ │
│  │ Planner │  │ gen2d / gen3d / remesh   │ │
│  │ Critic  │  │ uv_bake / validate /     │ │
│  │ 版本树   │  │ export / render_turntable│ │
│  └────┬────┘  └──────┬───────────────────┘ │
│       │         ┌────▼──────────────────┐  │
│       │         │ bpy headless 管线      │  │
│       │         └───────────────────────┘  │
└───────┼────────────────────────────────────┘
        │ Provider 抽象接口
┌───────▼────────────────────────────────────┐
│ meshy.py │ tripo.py │ rodin.py │ local Trellis │
└────────────────────────────────────────────┘
```

> Provider 层按账号状态支持 BYOK 直连与官方中转（Relay）双路由，详见 7.8。

### 7.2 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 桌面壳 | Electron + React + react-three-fiber + drei | 游戏行业 Windows 为主；生态成熟；three.js PBR+HDRI 预览足够判断"对不对" |
| 2D 交互 | 原生 Canvas / Konva.js（react-konva） | 贴图查看器、通道分离、mask 绘制（详见 7.7） |
| 本地服务 | Python + FastAPI（子进程打包） | bpy/trimesh/pymeshlab/rembg 全在 Python 生态 |
| 网格处理 | trimesh（检查）、pymeshlab（减面/重网格）、bpy（UV/烘焙/导出/渲染） | 全开源、可脚本化 |
| 3D 生成 | 云 API（Meshy/Tripo/Rodin），Provider 抽象 | MVP 质量最优解；接口收敛后可换本地 |
| 本地生成（P2） | TRELLIS（MIT 许可）、Hunyuan3D-2.1（备选） | 美术工作站常见 RTX 4090/24GB 可跑 |

### 7.3 3D 后处理管线（核心资产）

| 步骤 | 工具 | 说明 |
|---|---|---|
| 1 网格体检 | trimesh | 非流形、破洞、自交、组件数 |
| 2 修复 | trimesh/bpy | 补洞、去孤立组件、法线统一 |
| 3 减面/重拓扑 | pymeshlab（Instant Meshes 思路） | 硬表面道具四边面重拓扑效果良好 |
| 4 UV | bpy Smart UV Project / xatlas | 检测 UV 重叠 |
| 5 烘焙 | bpy bake | 从原始高模烘 normal/AO/curvature 到低模 |
| 6 校验 | 规则引擎（见 7.4） | FAIL 触发重试或标记 |
| 7 导出 | bpy | FBX/GLB + 贴图包 + 引擎预设 |
| 8 预览渲染 | bpy 转台渲染 | 供 Critic 与人工挑选 |

### 7.4 Game-ready 校验规则（MVP）

| 规则 | 默认阈值（可按资产类目配置） |
|---|---|
| 面数预算 | Prop LOD0 ≤ 5000 三角面（可调） |
| 四边面比例 | 硬表面 ≥ 85% |
| N-gon | 0 |
| UV 重叠 | 0（采样检测） |
| UV 岛间距 | ≥ 2px（4K 贴图基准） |
| 轴心 | 底面中心，可配置 |
| 真实单位 | 米制，与规格偏差 ≤ ±5% |
| 命名 | 引擎规范正则（如 `SM_Prop_*`） |
| 贴图 | 分辨率 2 的幂、通道用途标注 |

### 7.5 导出预设

- **Unity**：Y-up、米制、FBX；metallic-roughness → Unity Standard（metallic/smoothness 打包）
- **Unreal**：Z-up、厘米、FBX；ORM 打包贴图；`SM_`/`T_` 命名规范
- **通用**：GLB（PBR metallic-roughness 原生）

### 7.6 目录结构

```
asset-agent/
├─ apps/desktop/            # Electron + React + react-three-fiber
├─ services/agent/          # Python FastAPI sidecar
│  ├─ agent/                # planner / critic / 循环 / 版本树
│  ├─ tools/                # gen2d, gen3d, remesh, uv_bake, validate, export, render
│  └─ providers/            # meshy.py / tripo.py / rodin.py / local_trellis.py
├─ services/relay/          # 官方中转服务（Phase 2）：鉴权/计量/限流/转发
├─ recipes/                 # bpy 脚本、引擎导出预设、校验规则配置
└─ assets/styles/           # 风格圣经模板
```

### 7.7 视口与交互设计

**总体原则：预览与交互在前端，破坏性修改在后端，中间传"操作参数"而非数据流。**

| 操作 | 放哪 | 原因 |
|---|---|---|
| 相机、gizmo 摆放、显示模式、mask 绘制 | 前端 | 60fps 交互必须本地；全部是非破坏性的"视图状态" |
| 减面、重拓扑、UV、烘焙、修复 | 后端 bpy 管线 | 需要专业内核；每个操作 = 版本树新节点，撤销由版本树承担，无需自建 undo 栈 |
| 多资产摆放 | 前端记录 transform JSON，后端按需执行 | 状态可序列化、可回放 |
| 破坏性修改的撤销 | 版本树 | 前端永远只"显示某个版本"，不持有可变网格 |

#### 3D 检查视口（three.js + r3f + drei）

| 能力 | 实现 |
|---|---|
| 相机旋转/平移/缩放 | OrbitControls |
| 选中与移动/旋转/缩放（gizmo 拖拽） | TransformControls + raycast 拾取 |
| 线框 / clay / 材质通道（normal、AO、UV 检查格） | 材质 override 切换 |
| HDRI 环境预设（模拟 Unity/UE 默认打光） | Environment 预设 |
| 大网格拾取/框选 | three-mesh-bvh |
| 包围盒、尺寸标注、法线显示 | EdgesGeometry + 自绘 |

边界：**three.js 只做"检查与摆放"，不做顶点级网格编辑**——那是 DCC 的活，超出部分引导用户回 Blender/Maya，不与专业工具抢功能。

#### 2D 视图（Canvas / Konva）

- 贴图查看器：RGBA 通道分离、透明底棋盘格、像素吸管、放大镜
- 概念图 mask 绘制：画笔涂选区域 → 导出 PNG mask → 生成 API inpaint 局部重绘。美术对"圈出来重新生成"的接受度远高于改 prompt，是生成环节提升可控性的关键交互
- 裁剪/旋转/缩放：前端做预览，参数发给后端用 Pillow/OpenCV 执行，保证预览与管线产出一致

#### 2D/3D 联动定位（差异化细节）

3D 视口点选一个面 → raycast 取三角形索引 → 读 UV 属性算出所属 UV 岛范围 → 贴图查看器高亮该区域。解决美术高频操作"贴图哪里拉扯了"，实现成本低、感知价值高。

#### 格式与性能约定

- **预览格式统一 GLB**：sidecar 入库时把一切来源（FBX/OBJ/GLB）转成 GLB，前端不引 FBXLoader 等重 loader
- 资产库网格用 bpy 渲染的 PNG 缩略图，详情页才加载 three.js 视口
- 大网格生成 Draco 压缩副本供视口加载；拾取加速走 three-mesh-bvh
- WebGPU 渲染器暂不采用，WebGL2 足够

#### 为什么不嵌入 Blender

Blender 只以 headless 子进程 + 文件交换的方式使用（GPL 安全边界）；不做窗口内嵌：闭源商业产品链接 Blender 有 GPL 传染风险、包体 +300MB、窗口自动化脆弱。静态预览一律 bpy 渲 PNG，不使用 Blender 视口。

### 7.8 订阅与官方中转服务

**双模式路由**：Provider 抽象层按账号状态自动选择通路，上层 Agent/工具层无感知。规则：**已登录且已订阅 → 官方中转；未登录或未订阅 → BYOK（用户自填 Key）**。

| | BYOK（未登录/未订阅） | 官方中转（已订阅） |
|---|---|---|
| Key 来源 | 用户自填各 Provider Key | 官方持有，用户无感 |
| 计费 | 用户直接向 Provider 付费 | 扣订阅额度（credit 台账） |
| 使用门槛 | 用户自行注册/充值各平台 | 开箱即用，无跨境支付/网络门槛 |
| 责任与限流 | 用户自负 | 官方统一限流与故障处理 |
| 定位 | 信任通道 + 降级通道 | 主要收入来源 |

MVP 期实现 BYOK 并**预埋双路由接口**（路由开关是配置项）；账号/支付/中转服务在 Phase 2 落地——账号与支付合规会拖慢 MVP 节奏，且没有用户基数时订阅无从验证。

#### 中转服务（services/relay，Phase 2）

```
桌面端 ──HTTPS──▶ relay
                  ├─ 鉴权：账号会话 token（短时效 + 设备绑定）
                  ├─ 计量：credit 台账（授予/消耗/退款/调整），幂等键防重复扣费
                  ├─ 限流：账号级并发与速率（Redis）
                  ├─ 缓存：同设定图去重，压低官方成本
                  ├─ 退款：Provider 失败自动返还 credit
                  └─ 转发 ▶ Meshy / Tripo / Rodin / LLM API
```

技术选型：FastAPI + Postgres（账本）+ Redis（限流/缓存），与 sidecar 同栈降低维护成本。**双市场部署**：中转服务部署在海外（直连各 Provider），面向国内做优化接入线路——对国内用户，中转的核心价值正是解决跨境访问与支付两大门槛；对英语区用户，价值是免注册多家平台、统一收银。收款：国内微信/支付宝（需经营主体 + ICP 备案），海外 Stripe。登录双轨：手机号/微信（国内）+ 邮箱/Google（海外）。

企业版配套 **Web 管理后台**：成员/席位管理、团队额度池分配、用量审计报表（对账用），与 relay 共用账本；桌面端不承担企业管理功能。

#### 客户端配合

- 登录模块：双轨登录（国内手机号/微信，海外邮箱/Google），Electron `safeStorage`（OS 级加密）存储会话与用户自填 Key，自填 Key 永不上传
- 设置页：BYOK 管理（按 Provider 分栏、掩码显示、连通性自检按钮）；订阅状态与额度余额面板、用量流水
- 路由降级：中转不可用或额度耗尽时，明确提示并一键切回 BYOK（若有 Key）；BYOK 永久保留，不锁定用户

#### 安全与合规

- 计量以服务端台账为准，客户端额度仅作展示；任务提交带幂等键，失败自动退款
- **API 转售合规是前置条件**：中转本质是转售 Provider 能力，须逐一确认各 Provider ToS 或签分销/合作，未获许可的 Provider 不进中转列表（详见风险表）
- 支付与发票随市场选择（微信/支付宝 vs Stripe），国内经营涉及 ICP 备案；企业版走对公合同 + 发票，上线前备好合同与采购流程模板

---

## 8. 成本与商业模式

### 8.1 成本模型（估算，以官网报价为准）

- 云端 image-to-3D：约 ¥1–4 / 次生成（含贴图）；多变体策略下单件交付成本约 ¥3–12
- 一套 8 件资产 × 3 变体 ≈ ¥25–100；需在批量流程里做预算预估与同图缓存（同一设定图不重复计费）
- Critic 的 LLM/VLM 调用成本低于 ¥0.1 / 资产，可忽略
- 后处理管线（本地 bpy）零边际成本

### 8.2 定价草案（个人 / 企业双线，结构参考智谱 GLM 订阅体系）

**免费档（未登录/BYOK）**

- 工作流 C（纯后处理）完整可用；生成走用户自有 API Key
- 可选用增长策略：注册即赠少量中转额度（如 10 credits）作试用钩子

**个人版 Pro（自助订阅，月付/年付）**

- 自助注册、个人支付：国内微信/支付宝、海外 Stripe；月付/年付双档，年付享折扣（参考 GLM 个人版结构）
- 订阅含月度中转额度，额度定价 ≈ Provider 成本 × 1.3–1.5；超额购充值包（加油包），不强制订阅
- 双市场同结构：海外美元（Stripe）、国内人民币（微信/支付宝），档位换算预留支付通道费差异

**企业版 Enterprise（合同制）**

- 对公采购/合同制，增值税发票（国内）、invoicing（海外）；按席位年费 + 团队共享额度池
- 增值项：资产规范定制（按团队配置命名规范/校验阈值/导出预设）、Web 管理后台（成员席位管理、额度分配、用量审计）、SLA 与专属支持
- 安全诉求：可选私有化部署（本地 TRELLIS 版，数据不出机房）、素材不用于训练承诺、企业数据隔离——游戏工作室采购流程对"对公合同 + 发票 + 数据安全承诺"的依赖远高于价格本身

**BYOK 永久保留**：不产生中转毛利，但是信任建立与故障降级通道，不设障碍；企业用户同样可切 Provider 直连

### 8.3 合规要点

- 云 Provider 付费档才授予商用权——SDK 侧校验用户的订阅档位，文档明确告知
- Hunyuan3D 社区许可有附加条款，TRELLIS 为 MIT——本地化以 TRELLIS 优先
- 用户素材只读、生成结果归用户所有，写入 EULA
- 国内经营主体：**广州尼特之家信息技术有限公司**——以其名义办理 ICP 备案、微信/支付宝商户号、AIGC 合规评估，并登记桌面端/sidecar 软件著作权；EULA、官网与产品内版权署名同为公司主体

---

## 9. 里程碑

| 阶段 | 周期 | 内容 | 出口标准 |
|---|---|---|---|
| Phase 1 MVP | 8 周 | 见下分解 | 3–5 名外部美术跑通工作流 A，白模进引擎 |
| Phase 2 | +12 周 | 资产包批量、PBR 增强、LOD、本地 TRELLIS、风格圣经；账号/订阅支付/官方中转服务（+4–6 周，可与批量并行） | 单套 8 件资产批量交付；订阅用户全流程走中转扣费 |
| Phase 3 | Phase 2 后 | Unity/UE 插件、Blocker 批量、Substance 集成、绑定 API 接入 | 引擎内闭环 |

### 9.1 Phase 1 关键路径与并行轨道

**关键路径（不可压缩，决定最短工期）**

```
Provider 打通 → 生成落盘 → 修复/减面 → UV/烘焙 → 校验 → 导出 → 引擎实机验证
```

**并行轨道（可提前开工，不阻塞关键路径）**

| 轨道 | 内容 | 备注 |
|---|---|---|
| T1 前端 | Electron 壳、3D 检查视口、资产库、设置页 | 依赖 M8 数据模型冻结（W1） |
| T2 后端 | FastAPI sidecar 生命周期、任务队列、bpy/pymeshlab 管线 | 关键路径主体 |
| T3 规范 | 校验规则阈值、引擎导出预设、命名规范 | **W0 就要拿到真实阈值**，可用样例资产离线开发 |
| T4 产品/测试 | 黄金概念图集、验收用例、种子用户招募、埋点核对 | 全程并行 |

### 9.2 Phase 1 节点分解（8 周 + 1 周缓冲口径）

| 节点 | 周次 | 目标 | 交付物 | 出口标准 |
|---|---|---|---|---|
| **M0** 立项与验证启动 | W0（3–5 天） | 冻结范围与技术选型 | 5.4 范围冻结表评审通过；黄金概念图集 10 张冻结；Provider 盲测方案发出；Spike 脚本可跑 | 范围表签字；盲测已发出；Spike 环境就绪 |
| **M1** 骨架贯通 | W1–W2 | 壳 + sidecar + 视口 + Provider 抽象 | 可启动的 Electron 应用；sidecar 生命周期管理；GLB 视口；Meshy/Tripo 各跑通一次；四边面重拓扑 Spike 结论 | UI 内选图 → 云生成 → 落盘 GLB → 视口打开（暂无后处理）端到端跑通；**W2 末拍板 S1 去留** |
| **M2** 主链贯通 | W3–W4 | 修复/减面/UV 进管线 | 管线 v1（trimesh 修复 + pymeshlab 减面 + bpy UV）；任务队列与进度回传；中间产物落盘；资产状态机落地 | 概念图 → 生成 → 减面至预算内 → UV 零重叠 → GLB 导出，UI 内全流程完成；黄金集 10 张全部跑通 |
| **M3** 质量闭环 | W5–W6 | 烘焙 + 校验 + 报告 + 导出预设 + 转台 | 烘焙脚本；校验规则引擎（7 条）；校验报告 UI（点击定位高亮）；转台渲染；Unity/UE 导出预设；3D 检查视口 v1 | 黄金集通过率 ≥ 50%（首轮基线）；导出文件在 Unity/UE 实机导入成功；报告可定位到面/UV 岛 |
| **M4** 产品化闭环 | W7 | 资产库 / 版本树 / BYOK / 成本 | 资产库与缩略图；版本树与回滚；BYOK 设置与连通性自检；成本预估与预算上限；工作流 C 入口 | 关闭应用重开，资产与版本树完整；BYOK 错 Key/断网有明确提示；超预算被拦截 |
| **M5** 打磨与封测 | W8 | 稳定性与可用性 | 崩溃与错误处理；首次使用引导；安装包与更新通道；内部封测报告 | Release Gate G1–G7 达标；内部连续 20 次任务无崩溃 |
| **M6** 种子用户 alpha | W9（缓冲 + 外测） | 外部验证 | 3–5 名美术实测；问题清单；TTFE 与变体采纳率数据；alpha 复盘报告 | ≥3 人独立完成"概念图 → 引擎"；复盘报告给出 Phase 2 排序建议 |

> **口径说明**：上表按"3 人团队 + 8 周 + 1 周缓冲"排。若团队 < 3 人（或缺少技术美术角色），整体切换 12 周口径，并把 M7 视口收窄为"静态转台图 + 基础旋转"，S1–S7 全部移出 MVP（见 9.4）。

### 9.3 三个必须前置的决策点

| 决策点 | 时间 | 不决策的后果 |
|---|---|---|
| 四边面重拓扑是否进 MVP | **W2 末**（依据 M1 的 Spike 结果） | 拖垮关键路径；后期返工管线 |
| 校验阈值与引擎规范从哪来 | **W0**（访谈 2–3 位目标工作室美术，或采用公开规范） | M3 校验器与导出预设全部返工 |
| 云生成质量是否达"道具级可用" | **W2 末**（依据 M1 的盲测） | 若结论为否，立即启用 B 计划：MVP 重心前移到工作流 C（后处理 + 校验 + 导出），生成降级为可选模块 |

**黄金集纪律**：W0 冻结的 10 张概念图全程不得更换，作为每一节点的回归基线；每次节点验收都用同一批图跑，否则进度无法判断。

### 9.4 人力配置建议（8 周口径的最小可行团队）

| 角色 | 投入 | 职责 |
|---|---|---|
| 前端 / 桌面 | 1 人 | Electron + React + react-three-fiber，视口与资产库 |
| 后端 / 管线 | 1 人 | FastAPI sidecar、任务队列、bpy/pymeshlab 管线 |
| 技术美术（TA） | 0.5 人 | 校验规则、导出预设、引擎实机验证——**关键角色，缺此角色极易做出"能跑但美术不用"的产品** |
| 产品 / 测试 | 0.5 人 | 黄金集、验收用例、种子用户、埋点核对 |

合计约 **3 人 × 8 周 ≈ 24 人周**。2 人团队建议 12 周 + 砍掉全部 Should 项。

### 9.5 MVP 埋点与指标采集（对齐第 10 章）

MVP 必须就埋好最小事件集，否则 Phase 2 的指标无从校准：

- 任务维度：任务开始/结束、每一步耗时、失败原因分类
- 生成维度：生成次数、变体数、实际花费 vs 预估
- 挑选维度：变体是否被选中、选中第几个、是否重新生成
- 校验维度：每条规则 PASS/FAIL 明细、自动重试次数
- 导出维度：导出次数、引擎预设类型、是否实机使用（alpha 期问卷回访）

**落盘方式**：本地 JSONL，默认不上传（隐私优先）；alpha 期由用户手动导出后上报，用于第 10 章 TTFE、一次通过率、变体采纳率的首次测量。

---

## 10. 成功指标

| 指标 | 目标 |
|---|---|
| TTFE（概念图首图 → 首个导出文件） | P50 ≤ 10 分钟 |
| 校验器一次通过率（自动重试后） | ≥ 70% |
| 变体采纳率（美术至少选用一个变体进入后处理） | ≥ 50% |
| 交付资产进引擎率（导出后真实用于项目，问卷回访） | ≥ 40% |
| 种子用户 4 周留存 | ≥ 35% |

---

## 11. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 生成质量不达美术预期 | 高 | 变体制 + 工作流 C 兜底（即使生成不行，后处理仍有价值）；定位明确为白模/道具加速器 |
| 云 Provider API 变更/涨价 | 中 | Provider 抽象层，三家可切换；Phase 2 本地模型对冲 |
| 单件成本高，用户烧预算 | 中 | 生成前预估、月度预算上限、同图缓存、变体数可调 |
| 美术不信任 AI 产出 | 中 | 校验报告透明可查；不覆盖用户文件；从"脏活自动化"切入建立信任 |
| 商用授权纠纷 | 中 | 只接付费档云 API；本地走 MIT 许可模型；EULA 明确权属 |
| API 转售合规（Provider ToS 限制中转/转售） | 高 | 中转上线前逐一与 Provider 确认或签分销/合作；未获许可的不进中转列表，用户走 BYOK 直连 |
| 中转服务滥用/盗刷 | 中 | 设备绑定、账号级限流、异常用量监控、支付风控 |
| 国内 AIGC 合规（生成式 AI 管理办法/备案） | 中 | 以工具属性 + 已备案 Provider 能力定位；订阅上线前完成法务评估，必要时国内商业化延后、先海外验证 |
| 角色资产能力被质疑 | 低 | 范围明确排除，官网写清楚"Props & Environment" |

---

## 12. 未决问题（需拍板）

1. **商业模式路径**：先做独立商业产品（订阅制），还是先做工作室内部工具再产品化？影响部署形态（云 API 依赖 vs 私有部署优先）与合规投入。
2. **首选云 Provider**：Meshy（API 最全）还是 Rodin（质量上限高）？建议 MVP 双接并用同批概念图做盲测后定主 Provider。
3. **2D 模块**：是否在 v1 完全不做？目前方案为完全排除，风格设定图生成（工作流 B 内部用）除外。
4. **团队与预算**：当前可投入的人力规模，决定 Phase 1 是 8 周还是 12 周口径。**（W0 前置项：必须在 M0 节点前拍板，9.2 的节点表与 9.4 的人力口径均依赖此决策。）**
5. **工作流 C 是否免费**：免费引流 vs 直接收费，影响冷启动策略。
6. ~~市场选择~~ **已拍板：国内 + 英语地区双市场。** 支付双轨（微信/支付宝 + Stripe）、登录双轨（手机号/微信 + 邮箱/Google）、中转海外部署 + 国内优化线路；国内经营主体与 ICP 备案在订阅上线前完成，国内 AIGC 合规评估列为订阅上线前置项（见 8.3）。
7. **订阅额度定价**：个人版月/年档位与企业版席位价的 credit 锚定倍率（建议 1.3–1.5），Phase 2 启动前用真实成本数据校准；企业版年费口径待首批目标工作室访谈后定价。
8. **海外收款主体**：Stripe 不支持中国大陆主体（含广州尼特之家）直接开户，需在"设立香港/海外关联主体"与"Merchant of Record 代收（Paddle / Lemon Squeezy 等）"之间评估选择，与英语区定价、合同及 EULA 署名主体配套确定。
